%h1 Listing class_types

%table
  %thead
    %tr
      %th Name
      %th
      %th
      %th

  %tbody
    - @class_types.each do |class_type|
      %tr
        %td= class_type.name
        %td= link_to 'Show', class_type
        %td= link_to 'Edit', edit_class_type_path(class_type)
        %td= link_to 'Destroy', class_type, :method => :delete, :data => { :confirm => 'Are you sure?' }

%br

= link_to 'New Class type', new_class_type_path
