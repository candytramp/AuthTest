= form_for @class_type do |f|
  - if @class_type.errors.any?
    #error_explanation
      %h2= "#{pluralize(@class_type.errors.count, "error")} prohibited this class_type from being saved:"
      %ul
        - @class_type.errors.full_messages.each do |msg|
          %li= msg

  .field
    = f.label :name
    = f.text_field :name
  .actions
    = f.submit 'Save'
